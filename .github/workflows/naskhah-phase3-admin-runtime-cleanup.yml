name: Naskhah Studio Phase 3 Admin Runtime Cleanup

on:
  push:
    branches:
      - naskhah-phase3-app-modularization
    paths:
      - .github/workflows/naskhah-phase3-admin-runtime-cleanup.yml

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: naskhah-phase3-app-modularization
          fetch-depth: 0

      - name: Remove legacy admin runtime implementations
        run: node naskhah-studio/tools/scripts/cleanup-app-admin-runtime.mjs

      - name: Verify app.js syntax
        run: node --check naskhah-studio/app.js

      - name: Verify strict runtime guard
        run: node naskhah-studio/tools/scripts/check-runtime.mjs

      - name: Verify Phase 3 guard
        run: node naskhah-studio/tools/scripts/check-phase3-baseline.mjs

      - name: Commit cleanup
        run: |
          if git diff --quiet -- naskhah-studio/app.js naskhah-studio/tools/scripts/check-phase3-baseline.mjs; then
            echo "No H2 cleanup change detected."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add naskhah-studio/app.js naskhah-studio/tools/scripts/check-phase3-baseline.mjs
          git commit -m "Phase 3 H2: remove duplicated admin runtime implementations"
          git push origin HEAD:naskhah-phase3-app-modularization
