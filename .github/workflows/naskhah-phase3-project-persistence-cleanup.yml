name: Naskhah Studio Phase 3 Project Persistence Cleanup

on:
  push:
    branches:
      - naskhah-phase3-app-modularization
    paths:
      - .github/workflows/naskhah-phase3-project-persistence-cleanup.yml
      - naskhah-studio/tools/scripts/cleanup-app-project-persistence.mjs

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: naskhah-phase3-app-modularization
          fetch-depth: 0

      - name: Remove legacy saveProject implementation
        run: node naskhah-studio/tools/scripts/cleanup-app-project-persistence.mjs

      - name: Verify app.js syntax
        run: node --check naskhah-studio/app.js

      - name: Verify Phase 3 guard
        run: node naskhah-studio/tools/scripts/check-phase3-baseline.mjs

      - name: Commit cleanup
        run: |
          if git diff --quiet -- naskhah-studio/app.js naskhah-studio/tools/scripts/check-phase3-baseline.mjs; then
            echo "No G2 cleanup change detected."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add naskhah-studio/app.js naskhah-studio/tools/scripts/check-phase3-baseline.mjs
          git commit -m "Phase 3 G2: remove duplicated project persistence implementation"
          git push origin HEAD:naskhah-phase3-app-modularization
